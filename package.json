{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "umi-tools build",
    "pub": "yarn build && lerna publish"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.13.4",
    "umi-tools": "^0.4.0"
  },
  "dependencies": {
    "git-commit-msg-linter": "^2.3.2"
  }
}
